import java.util.List;
public class Database {
    TransactionManager transactionManager;
    List<DataManager> sites;
    // initialize the database status (read from file) and start executing by calling transactionManager.execute();
    public void start(String filePath)
    {

    }
    // initialize the database status (read from std) and start executing by calling transactionManager.execute();
    public void start()
    {
        
    }
    
}
